# Copia in .env e compila i valori. Il file .env è in .gitignore.

# Cloudflare Stream (video)
VITE_CLOUDFLARE_STREAM_CUSTOMER_CODE=

# Base URL del backend HQ (opzionale, default: https://hq.studiomobilitas.it)
# Il video "Iter selezione osteopata" viene recuperato via API dal gestionale (codice ITER_SELEZIONE_VSL).
VITE_API_BASE=
